# 2026-04-07 日记

## product_gen.py 重构
- 去掉了 CogView 文生图逻辑，全面改用 LibLib 工作流
- 新流程：原图 → 抠图(白底) → 生成背景(精修)
- 用 `WORKFLOWS` 字典管理工作流配置，`liblib_run()` 统一调用
- `extra_params` 支持额外节点参数，`{prompt}` 占位符自动替换智谱分析的 image_prompt
- mode: all / analyze / matting / background

## LibLib 工作流配置
- **抠图**: BiRefNet (自动抠图+去白边), workflow=565398da, template=4df2efa0, load_node=1
  - 之前用的 SegmentAnything 工作流会触发审核被拒
- **生成背景**: Flux.1 Kontext [pro], workflow=7e7b671b, template=4df2efa0, load_node=29
  - 节点27: LibLibTranslate (场景描述text, 用{prompt}占位)
  - 节点41: aspect_ratio=3:4

## 图床
- 0x0.st 已关闭上传服务
- 改用 litterbox.catbox.moe（临时图床，1小时有效期）

## 微信发图
- `MEDIA:` 语法在微信通道可能不工作（绝对路径被拦截）
- 相对路径 `MEDIA:./output/xxx.png` 可以
- `<qqmedia>` 是QQ专用标签，微信不适用

## LibLib 积分
- 当前余额约 334 积分
- 抠图消耗10积分，背景生成消耗约58积分
- 没有独立查余额接口，从任务返回的 accountBalance 读取
